<%= form_with(model: badge, local: true) do |form| %>
  <% if badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(badge.errors.count, "error") %> prohibited this badge from being saved:</h2>

      <ul>
        <% badge.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :remote_id %>
    <%= form.text_field :remote_id %>
  </div>

  <div class="field">
    <%= form.label :badge_url %>
    <%= form.text_field :badge_url %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :skill %>
    <%= form.text_field :skill %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
